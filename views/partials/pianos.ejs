<ul>
    <li>
        <p>Iterative tone</p>
        <div>
            <% for( let i = 0; i < 10; i++ ) { %>
                <button></button>
            <% } %>
        </div>

        <div>
            <% for( let i = 0; i < 9; i++ ) { %>
                <button></button>
            <% } %>
        </div>
    </li>

    <li>
        <p>Single tone</p>
        <button></button>
        <div>
            <% for( let i = 0; i < 10; i++ ) { %>
                <button></button>
            <% } %>
        </div>

        <div>
            <% for( let i = 0; i < 9; i++ ) { %>
                <button></button>
            <% } %>
        </div>
    </li>
</ul>